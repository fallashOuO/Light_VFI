# Light_VFI

A lightweight video frame interpolation project with:

- Base model (UNet2D)
- LoRA fine-tuning
- PyTorch offline inference
- PyTorch live inference (OpenCV display)
- ONNX export
- TensorRT engine support

## Training (Base)